name: Build BulletinPro - Linux & Windows

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  BUILD_NUMBER: 1
  BUILD_VERSION: 1.0.0
  PYTHON_VERSION: 3.12.8
  FLET_CLI_VERSION: 0.27.5
  FLUTTER_VERSION: 3.30.0  # Version avec Dart 3.7+
  PYTHONUTF8: 1
  FLET_CLI_NO_RICH_OUTPUT: 1
  UV_NO_PROGRESS: 1

jobs:
  build-linux:
    name: 🐧 Build Linux (.deb)
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: 🦋 Setup Flutter ${{ env.FLUTTER_VERSION }}
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
      
      - name: 📦 Install flet-cli ${{ env.FLET_CLI_VERSION }}
        run: |
          python -m pip install --upgrade pip
          pip install flet-cli==$FLET_CLI_VERSION
      
      - name: 🔧 Install Linux dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ninja-build libgtk-3-dev
      
      - name: 📚 Install Python project dependencies
        run: |
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi
      
      - name: 🔨 Build Linux Application
        run: |
          flet build linux --verbose --build-number=$BUILD_NUMBER --build-version=$BUILD_VERSION
      
      - name: 🔍 Verify build output
        run: |
          echo "=== Contents of build/linux ==="
          ls -lhR build/linux || echo "build/linux not found"
          
          echo ""
          echo "=== Searching for .deb files ==="
          find build -name "*.deb" -type f || echo "No .deb files found"
      
      - name: 📤 Upload Linux Artifact
        uses: actions/upload-artifact@v4
        with:
          name: bulletinpro-linux-build
          path: build/linux
          if-no-files-found: error

  build-windows:
    name: 🪟 Build Windows (.exe)
    runs-on: windows-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: 🦋 Setup Flutter ${{ env.FLUTTER_VERSION }}
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
      
      - name: 📦 Install flet-cli ${{ env.FLET_CLI_VERSION }}
        run: |
          python -m pip install --upgrade pip
          pip install flet-cli==$env:FLET_CLI_VERSION
      
      - name: 📚 Install Python project dependencies
        run: |
          if (Test-Path requirements.txt) {
            pip install -r requirements.txt
          }
      
      - name: 🔨 Build Windows Application
        run: |
          flet build windows --verbose --no-rich-output --build-number=$env:BUILD_NUMBER --build-version=$env:BUILD_VERSION
      
      - name: 🔍 Verify build output
        run: |
          Write-Host "=== Contents of build/windows ==="
          if (Test-Path build/windows) {
            Get-ChildItem -Path build/windows -Recurse | Format-Table -AutoSize
          } else {
            Write-Host "build/windows not found"
          }
      
      - name: 📤 Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: bulletinpro-windows-build
          path: build/windows
          if-no-files-found: error

  create-release:
    name: 🚀 Create Release
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    permissions:
      contents: write
    
    steps:
      - name: 📥 Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: bulletinpro-linux-build
          path: ./release/linux
      
      - name: 📥 Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: bulletinpro-windows-build
          path: ./release/windows
      
      - name: 📦 Create distribution packages
        run: |
          cd release/linux
          if ls *.deb 1> /dev/null 2>&1; then
            cp *.deb ../../BulletinPro-Linux.deb
          fi
          cd ../..
          
          cd release/windows
          if [ -d x64 ] || [ -d runner ]; then
            zip -r ../../BulletinPro-Windows.zip ./*
          fi
          cd ../..
      
      - name: 🚀 Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            BulletinPro-Linux.deb
            BulletinPro-Windows.zip
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## 📦 BulletinPro ${{ github.ref_name }}
            
            ### 📥 Installation
            
            **Linux (Ubuntu/Debian):**
            ```bash
            sudo dpkg -i BulletinPro-Linux.deb
            ```
            
            **Windows:**
            1. Téléchargez `BulletinPro-Windows.zip`
            2. Extrayez le contenu
            3. Lancez l'exécutable
            
            ### 📝 Notes
            - Version: ${{ env.BUILD_VERSION }}
            - Build: ${{ env.BUILD_NUMBER }}
            - Python: ${{ env.PYTHON_VERSION }}
            - Flutter: ${{ env.FLUTTER_VERSION }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-summary:
    name: 📊 Build Summary
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: 📊 Generate Summary
        run: |
          echo "## 🎉 BulletinPro Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Build Configuration:**" >> $GITHUB_STEP_SUMMARY
          echo "- Version: $BUILD_VERSION" >> $GITHUB_STEP_SUMMARY
          echo "- Build Number: $BUILD_NUMBER" >> $GITHUB_STEP_SUMMARY
          echo "- Python: $PYTHON_VERSION" >> $GITHUB_STEP_SUMMARY
          echo "- Flet CLI: $FLET_CLI_VERSION" >> $GITHUB_STEP_SUMMARY
          echo "- Flutter: $FLUTTER_VERSION" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Platform | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🐧 Linux | ${{ needs.build-linux.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🪟 Windows | ${{ needs.build-windows.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ needs.build-linux.result }}" = "success" ] && [ "${{ needs.build-windows.result }}" = "success" ]; then
            echo "### ✅ Tous les builds ont réussi!" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📦 **Téléchargez les artifacts** dans la section ci-dessous." >> $GITHUB_STEP_SUMMARY
          else
            echo "### ⚠️ Certains builds ont échoué" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Consultez les logs des jobs en erreur pour plus de détails." >> $GITHUB_STEP_SUMMARY
          fi
